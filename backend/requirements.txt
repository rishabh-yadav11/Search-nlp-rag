fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
aiomysql==0.3.0
redis==5.2.1
qdrant-client==1.11.3
fastembed==0.8.0
sentence-transformers==5.3.0
torch==2.13.0
transformers==5.5.4
onnxruntime==1.28.0
# ONNX cross-encoder (optimum[onnxruntime]) is intentionally NOT installed:
# optimum-onnx pins transformers<4.58, which conflicts with the pinned
# transformers 5.5.4 (required to fix CVE-2026-4372 / CVE-2026-5241 /
# CVE-2026-1839). The reranker falls back to the torch CrossEncoder
# automatically (app/reranker.py).
openai==1.68.2
python-dotenv==1.2.2
cachetools==5.5.0
tqdm==4.66.5
aiosqlite==0.20.0
bcrypt==4.2.1
symspellpy==6.10.0
# Fallback tz database for stdlib zoneinfo (app/query_intent.py resolves
# Asia/Kolkata); used when the container has no system /usr/share/zoneinfo.
tzdata==2026.3
