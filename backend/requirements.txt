fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
aiomysql==0.2.0
redis==5.2.1
qdrant-client==1.11.3
fastembed==0.8.0
sentence-transformers==3.1.1
torch==2.13.0
transformers==4.57.6
onnxruntime==1.28.0
# Optional (falls back to torch rerank when missing): ONNX cross-encoder path.
# The [onnxruntime] extra is required — base optimum does not ship
# `optimum.onnxruntime`.
optimum[onnxruntime]>=1.20.0
openai==1.68.2
python-dotenv==1.0.1
cachetools==5.5.0
tqdm==4.66.5
aiosqlite==0.20.0
bcrypt==4.2.1
# Query-string typo correction (app/query_fix.py) — pure Python, no build deps.
symspellpy==6.10.0
