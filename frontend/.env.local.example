# API base URL. Defaults to http://localhost:8000 when unset.
# Use a relative/empty value or location.origin when the API is served
# same-origin (e.g. behind nginx on the same port as this app).
NEXT_PUBLIC_API_BASE=http://localhost:8000